'use server'

// Placeholder — fully implemented in Phases 8 & 9
// All AI calls are server-side only — API keys never exposed to browser

export async function enhanceDocument(_content: string, _patientContext: Record<string, unknown>) {
  void _content
  void _patientContext
  // Phase 8
}

export async function analyzeLabReport(_fileUrl: string, _patientContext: Record<string, unknown>) {
  void _fileUrl
  void _patientContext
  // Phase 9
}
